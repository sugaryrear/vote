{%  extends 'admin_layout.twig' %}

{% block content %}
    <div class="login-box">
        <div class="card border-0 shadow-sm">
            <div class="card-body">
                <div class="text-center mb-3">
                    <i class="fas fa-user-circle fa-5x text-primary"></i>
                </div>

                <div class="mb-5 text-center">
                    <h4 class="text-center mb-0 text-primary">Dashboard</h4>
                    <span class="text-muted small">Authenticate</span>
                </div>

                {%  if error is defined %}
                    <p class="text-danger text-center">{{ error }}</p>
                {% endif %}

                <form method="POST" action="{{ url('admin/login/authenticate') }}">
                    <div class="form-group">
                        <label for="code" class="text-muted small">
                            Enter the 6 digit code from your authenticator app
                        </label>
                        <input type="number" class="form-control" name="code" id="code" 
                        placeholder="Code">
                    </div>

                    <input type="hidden" name="_token" value="{{ csrf_token }}">

                    <div class="form-group text-center mb-0">
                        <input type="submit" value="Continue" class="btn btn-primary btn-block mb-2">
                        <a href="{{ url('admin') }}">Cancel</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
{% endblock %}