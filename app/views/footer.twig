{% block footer %}
<div class="container mt-3">
    <div class="row">
        <div class="col-sm-12 col-lg-6">
            <p class="text-muted">
                Copyright &copy; {{ curdate("Y") }}
                <a href="{{ url('') }}">{{ constant('site_title')}}</a>.
                Created by <a href="https://rune-nexus.com" target="_blank"> King Fox</a>.
            </p>
        </div>
        <div class="col-sm-12 col-lg-6">
            <p class="text-right">
            {% if constant("show_admin") %}
            <a class="text-danger" href="{{ url('admin') }}">
                <span class="nav-link-inner--text">
                <i class="fas fa-cog"></i> Admin
                </span>
            </a>
            {% endif %}
            </p>
        </div>
    </div>
</div>
{% endblock %}